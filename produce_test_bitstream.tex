You have now installed docker and pulled the required image. The next step is to run the image and produce a test bitsream to verify that everything is working correctly.

\begin{minted}{bash}
    docker run --rm -it --platform linux/amd64 f4pga-docker /bin/bash
\end{minted}